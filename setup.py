"""Legacy setuptools entrypoint.

This project is primarily configured via pyproject.toml.
setup.py is kept for compatibility with older tooling.
"""

from setuptools import setup

if __name__ == "__main__":
    setup()
